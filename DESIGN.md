# DESIGN.md — 디자인 시스템 단일 출처

`AGENT.md`가 링크하는 디자인 표준. `design` 에이전트가 토큰을 **확장**하고,
`implementation` 에이전트가 토큰을 **적용**한다(생값 hex/px 금지). 작업별
스펙은 이 표준을 따르며, 벗어남만 스펙의 `design_ref`에 기록한다.

## 토큰 <!-- TODO: 실제 값으로 채울 것 -->

토큰은 컴포넌트 독립적으로 유지한다. 위치/포맷은 레포 컨벤션을 따른다
(CSS 커스텀 프로퍼티 / Tailwind theme / 디자인 토큰 JSON 중 하나).

### 색 (시맨틱 역할)

| 토큰 | 역할 | 값 | 대비 |
|---|---|---|---|
| `--color-fg` | 본문 텍스트 | `<...>` | WCAG AA 충족 |
| `--color-bg` | 배경 | `<...>` | — |
| `--color-accent` | 주요 액션 | `<...>` | WCAG AA 충족 |

> 텍스트·핵심 UI 색은 WCAG AA 대비를 토큰 레벨에서 보장한다. AA를 못 맞추면
> 예외로 두지 말고 토큰을 고친다(`accessibility` 에이전트가 이 기준으로 검증).

### 간격 / 타이포 / radius / shadow / 모션

<!-- TODO: 스케일 정의 -->

## 컴포넌트 프리미티브 <!-- TODO -->

기존 공유 컴포넌트를 재사용·확장한다. 평행한 새 시스템을 만들지 않는다.
