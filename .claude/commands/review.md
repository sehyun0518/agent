---
description: 이미 만들어진 변경을 판정만 한다. 구현하지 않으므로 red 게이트가 없다. 대신 **테스트 증거가 없으면 판정하지 않는다** — 없는 증거를 review가 대신 만들지 않는다.
---

`workflows/review.yaml`를 읽고 그 순서대로 진행한다. 단계 7개다.

각 step의 `expect`·`expectAnyOf`가 전이 조건이고, `gate`가 가리키는
`workflows/gates/*.md`가 무엇을 봐야 하는지 정한다. 계층은 전부 필수다 —
건너뛰는 것이 아니라 계약 고정 단계의 `test.<layer>.applicability` 판정이
`not-applicable`일 때 넘어간다 (ADR-0038).

순서를 여기 옮겨 적지 않는다. 워크플로 파일이 단일 출처다.

**자동이 아닌 것.** 이 저장소에는 워크플로 실행 엔진도 증거 저장소도 없다
(ADR-0002). 단계 호출과 증거 기록은 사람 또는 메인 에이전트가 한다. 게이트가
증거를 본다고 돼 있으면 그 증거를 실제로 남겼는지 직접 확인해야 한다.

Git 작업은 이 흐름에 자동으로 붙지 않는다. `/git-commit`처럼 따로 부른다.
