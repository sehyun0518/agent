---
description: 머지 전에 답하지 않은 리뷰 지적과 진행 중인 체크를 센다. 남아 있으면 0이 아닌 코드로 끝난다.
---

```bash
npm run reviews -- <pr번호>
```

GitHub에서 그때그때 읽는다. **막지 않는다** — 머지 여부는 사람이 정하고, 이 명령은 아직 볼 것이 남았는지만 말한다 (ADR-0034).

이 파일에 내용을 옮겨 적지 않는다.
